// 后端地址（唯一需要改的地方）
//
// 1. 开发者工具 + 本机后端：保持 http://127.0.0.1:8000 即可，
//    并在「详情 → 本地设置」勾选「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」。
// 2. 真机调试：改成运行后端的电脑在局域网内的 IP，例如 http://192.168.1.100:8000
//    （手机与电脑需在同一 Wi-Fi 下，同样需勾选不校验合法域名）。
// 3. 正式上线：必须使用已备案的 HTTPS 域名，并在小程序后台配置 request/uploadFile/downloadFile 合法域名。
const BASE_URL = 'http://127.0.0.1:8000';

module.exports = {
  BASE_URL
};
